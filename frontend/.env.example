# DataForSEO API Credentials (Required for live data)
# Server-side environment variables (no VITE_ prefix)
DATAFORSEO_LOGIN="your-email@example.com"
DATAFORSEO_PASSWORD="your-api-password"

# YouTube Data API v3 (Optional - for accurate channel video counts)
# Get your API key from: https://console.cloud.google.com/apis/credentials
# Enable "YouTube Data API v3" in your Google Cloud Console
#
# QUOTA INFO: Free tier = 10,000 units/day. Each channel search = ~100-200 units.
# The app caches results for 24 hours to reduce API usage.
#
# PRIMARY KEY (required for YouTube features)
YOUTUBE_API_KEY="your-youtube-api-key"
#
# ADDITIONAL KEYS (optional - for automatic rotation when quota exceeded)
# Create multiple Google Cloud projects for more quota
# YOUTUBE_API_KEY_2="your-second-api-key"
# YOUTUBE_API_KEY_3="your-third-api-key"

# ========================================
# CORS / Security (Production)
# ========================================
# Comma-separated list of allowed origins for CORS
# ALLOWED_ORIGINS="https://yourdomain.com,https://www.yourdomain.com"

# Your production URL (used as fallback for CORS)
# PRODUCTION_URL="https://yourdomain.com"

# ========================================
# DATABASE (Optional - for saving projects)
# ========================================
# The app works without a database using sample data.
# Add these only if you want to save projects.

# DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"
# DIRECT_URL="postgresql://user:password@host:5432/database?sslmode=require"

# For Vercel Postgres:
# DATABASE_URL="${POSTGRES_PRISMA_URL}"
# DIRECT_URL="${POSTGRES_URL_NON_POOLING}"
